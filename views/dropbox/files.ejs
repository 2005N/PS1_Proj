<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dropbox Files</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Your Dropbox Files</h1>

        <% if (files && files.length > 0) { %>
            <% files.forEach(file => { %>
                <div class="file-item">
                    <span><%= file.name %></span>
                    <a href="/dropbox/download?token=<%= token %>&path=<%= encodeURIComponent(file.path_lower) %>">Download</a>
                </div>
            <% }) %>
        <% } else { %>
            <p>No files found in your Dropbox.</p>
        <% } %>
    </div>
</body>
</html>